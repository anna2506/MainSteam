import * as types from '../common/actionTypes/steam'

export const initialState = {
}

export default function steam(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}
